language: generic

sudo: false

addons:
  apt:
    sources:
    - cassou-emacs
    packages:
    - emacs24

env:
  matrix:
    - EMACS=emacs24

script:
  $EMACS --version &&
  $EMACS -Q -batch -L . -l redpen-paragraph-test.el -f ert-run-tests-batch-and-exit

# Local Variables:
# coding: utf-8
# End:
